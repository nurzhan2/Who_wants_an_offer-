# Alembic configuration. Lives at the repository root so that
# `alembic upgrade head` works from where every other command is run.
#
# The database URL is deliberately absent: env.py reads it from
# app.core.config.settings, so there is exactly one place that knows it and no
# credential can end up in a tracked file.

[alembic]
script_location = backend/alembic
prepend_sys_path = backend
path_separator = os
file_template = %%(rev)s_%%(slug)s
timezone = UTC

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
