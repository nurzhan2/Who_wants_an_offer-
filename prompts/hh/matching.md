# МЕГАПРОМТ — скоринг соответствия. Недостающее звено проекта.

Вставлять целиком, в чистой сессии Claude Code. Приоритет выше дашборда
из MEGAPROMPT-hh-final.md: без этого весь проект не делает того, ради
чего сделан.

---

```
Прочитай CLAUDE.md, docs/MATCHING.md, docs/ARCHITECTURE.md,
backend/app/schemas/match.py, backend/app/db/repositories/match.py,
backend/app/matching/embeddings.py, backend/app/sources/hh.py.

═══════════════════════════════════════════════════════════════════
ЧТО ИЗМЕРЕНО В БАЗЕ 7 сентября 2026
═══════════════════════════════════════════════════════════════════

  vacancy               643 строки, из них 515 с эмбеддингами
  candidate_profile     1
  profile_skill         20
  vacancy_skill         0        ← пусто
  match                 60       ← происхождение неизвестно, проверить:
                                   похоже на сиды, а не на расчёт
  scripts/run_matching.py         отсутствует

ДВА ПРОБЕЛА, оба обязательны:

1. КОДА, КОТОРЫЙ СЧИТАЕТ SCORE, НЕТ НИ СТРОКИ. Есть таблица match,
   схемы, MatchRepository.bulk_upsert — и никакого расчёта. `wwao match`
   честно об этом сообщает и ничего не делает.

2. vacancy_skill ПУСТА. Коннектор hh извлекает keySkills и кладёт их
   в raw._derived.key_skills, но в таблицу навыков они не попадают.
   Это критично: у hh навыки приходят СТРУКТУРИРОВАННЫМ списком, а не
   текстом, поэтому пересечение «требуемое ∩ имеющееся» здесь точное,
   а не восстановленное из описания. Ради этого преимущества всё и
   затевалось, и оно сейчас не используется.

ДЕЛАТЬ В ЭТОМ ПОРЯДКЕ: сначала заполнение vacancy_skill, потом скоринг.
Скоринг без навыков выродится в одну лишь косинусную близость.
```

```
═══════════════════════════════════════════════════════════════════
ЗАДАЧА 1. Заполнить vacancy_skill.
═══════════════════════════════════════════════════════════════════

Источник — то, что коннектор уже извлёк: raw._derived.key_skills.
Писать в vacancy_skill в том же шаге пайплайна, где сохраняется вакансия,
плюс разовый бэкфилл для 643 уже собранных строк.

ЧТО НЕ ЯВЛЯЕТСЯ НАВЫКОМ. hh подмешивает в keySkills требования к языкам
строками вида «Русский — C1 — Продвинутый» (измерено: 9 из 121 на одной
выборке). В коннекторе это уже разделено — есть отдельное поле
language_requirements. Убедиться, что в vacancy_skill языки не попадают:
иначе кандидат без строки «Казахский — C2» будет выглядеть как человек,
у которого не хватает навыка.

НОРМАЛИЗАЦИЯ. У hh навыки — свободный текст на двух языках вперемешку
(«Python», «Работа в команде», «1С: Управление персоналом», «REST API»).
На стороне кандидата в profile_skill 20 записей своего формата.
Пересечение по точному равенству строк даст почти нули. Нужна
нормализация, но БЕЗ выдумывания: приведение регистра, обрезка пробелов,
разумный набор синонимов (postgres/postgresql, js/javascript). Не
использовать LLM для сопоставления навыков в основном пути: это
непредсказуемо, дорого и не воспроизводится в тестах.
Что нормализация не свела — считать непокрытым и показывать честно.

═══════════════════════════════════════════════════════════════════
ЗАДАЧА 2. Скоринг: scripts/run_matching.py и `wwao match`.
═══════════════════════════════════════════════════════════════════

Формулу НЕ ИЗОБРЕТАТЬ: она описана в docs/MATCHING.md, следовать ей.
Если документ расходится с тем, что возможно на имеющихся данных —
исправить документ и сказать об этом в отчёте, а не молча разойтись.

Форма скрипта — как у scripts/run_pipeline.py и generate_letters.py,
запуск через `python scripts/run_matching.py`, обёртка в `wwao match`
уже ждёт именно этого.

ОБЯЗАТЕЛЬНО СОХРАНЯТЬ ОБЪЯСНЕНИЕ, а не только число: какие требуемые
навыки покрыты, какие нет, вклад семантической близости. Его читают
и генератор писем (письмо строится вокруг пересечения), и дашборд
(колонка «почему»), и человек в карточке подтверждения перед отправкой.
Число без объяснения бесполезно во всех трёх местах.
```

```
═══════════════════════════════════════════════════════════════════
ИЗМЕРЕННЫЕ ОСОБЕННОСТИ ДАННЫХ. Учесть, иначе скоринг соврёт.
═══════════════════════════════════════════════════════════════════

ЗАРПЛАТЫ ПОЧТИ НЕТ. Замер: 5 вакансий из 6 приходят с
{"noCompensation": {}}. Отсутствие зарплаты НЕ должно понижать score и
не должно отсеивать вакансию. Иначе корпус схлопнется до одной шестой.
Внимание: {"noCompensation": {}} — непустой dict, проверка `if salary:`
его пропустит.

ЭМБЕДДИНГИ ЕСТЬ НЕ У ВСЕХ: 515 из 643. Вакансия без вектора должна
получать score по навыкам с честной пометкой «семантика не посчитана»,
а не молча ноль и не исключение из выдачи.

responsesCount / totalResponsesCount ЛЕЖАТ В ДАННЫХ и в скоринге пока
не используются. Вакансия со 114 откликами и вакансия с 12 — разные
шансы при одинаковом соответствии. Ввести как отдельный видимый фактор
или как отдельную колонку в дашборде, но НЕ подмешивать тихо в score:
владелец должен видеть, что на что повлияло.

company.employerOnAdditionalCheck — работодатель на доппроверке hh.
Полезный признак, показывать в карточке.

60 СТРОК В match НЕИЗВЕСТНОГО ПРОИСХОЖДЕНИЯ. Проверить, откуда они.
Если это сиды — не давать им попадать в очередь на отклик, как уже
сделано для hh-dev-* строк. Настоящий расчёт не должен молча
перезаписывать их вперемешку с сидовыми.

═══════════════════════════════════════════════════════════════════
DEFINITION OF DONE
═══════════════════════════════════════════════════════════════════

- [ ] `wwao match` считает и записывает score по всем 643 вакансиям
- [ ] vacancy_skill заполнена, языки в неё не попали
- [ ] бэкфилл навыков для уже собранных вакансий выполнен
- [ ] у каждого match сохранено объяснение: покрытые и непокрытые навыки
- [ ] вакансии без зарплаты и без эмбеддинга не проваливаются в ноль,
      и на это есть тесты
- [ ] `wwao queue` показывает score вместо `--`
- [ ] ruff, mypy --strict, весь прогон зелёные
- [ ] тесты не ходят в сеть

ЧЕГО НЕ ДЕЛАТЬ:
- не изобретать формулу мимо docs/MATCHING.md
- не сопоставлять навыки через LLM в основном пути
- не подмешивать число откликов в score незаметно
- не считать отсутствие зарплаты недостатком вакансии
- упёрся в границу — остановиться и написать в отчёте

ПОСЛЕДНЕЕ. `wwao match` сейчас честно объясняет, чего не хватает, вместо
того чтобы притвориться работающим. Это правильное поведение, и его
стоит сохранить как образец: когда звено отсутствует, CLI говорит об
этом прямо, а не возвращает пустой результат как успех.
```
