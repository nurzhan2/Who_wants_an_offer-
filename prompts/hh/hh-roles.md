# МЕГАПРОМТ — обход по профессиям, а не подряд

Вставлять целиком, в чистой сессии Claude Code. Приоритет выше дашборда.
Без этого весь пайплайн работает вхолостую: он собирает не те вакансии.

---

```
Прочитай CLAUDE.md, docs/SOURCES.md § hh, backend/app/sources/hh.py,
backend/app/matching/scorer.py, docs/MATCHING.md.

═══════════════════════════════════════════════════════════════════
ИЗМЕРЕНО 8 сентября 2026. Это и есть постановка задачи.
═══════════════════════════════════════════════════════════════════

Скоринг заработал против настоящего профиля владельца (junior Python
Developer, backend/AI, 1.1 года, 66 навыков). Результат по 643 вакансиям:

  откликаться          0
  сильное совпадение   0
  на вырост            1
  мимо                 481
  отфильтровано      100

Верх списка:
  69.7  Intern Developer                          (отфильтрована)
  61.3  Менеджер по продажам b2b
  58.7  Менеджер по продажам B2B
  54.0  Менеджер по продажам
  50.6  Стажер (fresh graduates)
  50.3  Бухгалтер / Юрист / Разнорабочий / Профориентолог …

Плотность у 50.3 — это базовый уровень «город подходит, опыт не выше
требуемого» без единого совпавшего навыка. Скоринг честен, мерить нечего.

ПРИЧИНА: обход sitemap идёт строго по свежести и СЛЕП К ПРОФЕССИИ,
выбирая равномерно из ~13 557 вакансий Алматы. Из 294 собранных hh-строк
в семействах ролей «программист/разработчик/devops» — НОЛЬ. Топ ролей:
продажи 34, учителя 14, стройка. 388 из 643 вообще без указанных навыков.

ЗАДАЧА: собирать вакансии, связанные с Python-разработкой, а не что
попало. Владелец просит брать ШИРОКО — всё, что относится к разработке
и смежному, — а сузить можно потом фильтром на своей стороне.
Переусердствовать с узостью хуже, чем захватить лишнее.
```

```
═══════════════════════════════════════════════════════════════════
ГЛАВНАЯ ТРУДНОСТЬ, назвать её прямо перед тем, как проектировать.
═══════════════════════════════════════════════════════════════════

В sitemap у записи есть ТОЛЬКО <loc> и <lastmod>. Профессии там нет.
professional_role_ids извлекаются из страницы вакансии, то есть уже
ПОСЛЕ того, как запрос потрачен. Значит «фильтровать по роли при обходе»
в лоб невозможно: отсев после загрузки экономит место в базе, но не
меняет главного — доли релевантных вакансий на потраченный запрос.

ЕСТЬ КАНДИДАТ НА РЕШЕНИЕ, и он уже был замечен разведкой, но отложен.
В sitemap-индексе рядом с vacancy0..9.xml лежат vacancies0..14.xml —
это НЕ те же данные. Замер 6 сентября: vacancies0.xml для Алматы —
858 КБ, 5716 URL вида
    https://almaty.hh.kz/vacancies/crm-marketolog
без строки запроса и без lastmod. Похоже на каталожные страницы
по профессиям.

ЭТАП 0 — ИЗМЕРИТЬ, НЕ ПРЕДПОЛАГАТЬ. Обязателен, к коду не переходить.

1. Загрузить vacancies0..14.xml, собрать все слаги. Найти те, что
   относятся к разработке: python, backend, razrabotchik, programmist,
   devops, data, ml и подобные. Выписать список фактических слагов.
2. Загрузить ОДНУ такую страницу и проверить: есть ли в ней
   HH-Lux-InitialState и лежит ли внутри список вакансий с их id.
   Замерить: сколько вакансий на странице, есть ли пагинация без
   строки запроса, обновляется ли состав.
3. Отдельно: api.hh.ru/professional_roles отдаёт 200 (проверено) —
   это официальный справочник ролей с идентификаторами. В рекламной
   телеметрии hh наблюдался параметр roles=96, похоже на
   «Программист, разработчик». Сверить со справочником.

РАЗВИЛКА ПО РЕЗУЛЬТАТУ ЭТАПА 0:

  Если каталожные страницы отдают списки id — это и есть вход по
  профессии. Обход становится: слаги ролей → страницы каталога → id →
  /vacancy/{id}. Ни одной строки запроса, всё внутри robots.

  Если не отдают — честно написать об этом в отчёте и реализовать
  запасной путь: обход как сейчас, но с ранним отсевом по
  professional_role_ids сразу после загрузки, с сохранением только
  релевантных. Прямо указать в докстринге, что запросы это НЕ экономит,
  и что доля релевантных на запрос остаётся прежней. Не выдавать этот
  вариант за решение исходной задачи.

НЕ ИЗОБРЕТАТЬ ТРЕТИЙ ПУТЬ через поиск: /search/vacancy?... запрещён
robots и это правило не обсуждается.
```

```
═══════════════════════════════════════════════════════════════════
КАК ВЫБИРАТЬ РОЛИ. Широко.
═══════════════════════════════════════════════════════════════════

Владелец: junior Python Developer, backend и AI-интеграции. Брать надо
не только «Python», а весь смежный круг: бэкенд на любом языке, стажёр
и джуниор разработчика, data/ML, DevOps, интеграции, автоматизация,
QA-автоматизация. Лишнее отсеет скоринг — он для этого и написан, и на
корпусе из своей области он наконец начнёт разделять, а не выдавать
всем подряд базовые 50.3.

Набор ролей вывести из активного профиля, а не хардкодить под одного
человека: у профиля есть headline, seniority и 66 навыков. Соответствие
«профиль → роли» держать в конфиге рядом с hh_sites.yaml, как уже
сделано для городов, а не в коде.

ПОРЯДОК ОБХОДА в пределах бюджета прогона:
  1. вакансии выбранных ролей, новые сверху по lastmod
  2. остаток бюджета — общий обход как сейчас
Второй пункт сохранить: он даёт корпусу разнообразие и подстраховывает,
если подбор ролей окажется слишком узким.

ЧТО НЕ МЕНЯТЬ:
- requests_per_second = 0.25 с джиттером. Замерено: 0.73 rps → капча на
  172-м постинге, 1.02 rps → на 50-м, 0.25 rps → 15 минут без капчи.
  Соблазн ускориться ради нового корпуса будет; ускоряться нельзя.
- точную отметку позиции через record_progress
- обработку капчи и разделение challenge/robots
- анонимность краулера: ни кук, ни аккаунта, ни браузера

═══════════════════════════════════════════════════════════════════
DEFINITION OF DONE
═══════════════════════════════════════════════════════════════════

- [ ] Этап 0 выполнен, результаты записаны в docs/SOURCES.md § hh
      как измерения с датой, а не как предположения
- [ ] прогон приносит вакансии из ролей разработки; в отчёте видно,
      сколько релевантных на потраченный запрос — до и после
- [ ] `wwao match` после наполнения даёт распределение, где верх списка
      это разработка, а не менеджеры по продажам
- [ ] соответствие «профиль → роли» в конфиге, не в коде
- [ ] позиция обхода по-прежнему записывается, темп не поднят
- [ ] ruff, mypy --strict, весь прогон зелёные; тесты без сети

ЧЕГО НЕ ДЕЛАТЬ:
- не ходить в /search/vacancy и не добавлять строк запроса на hh
- не поднимать темп
- не сужать набор ролей до одного «Python»: просили широко
- не выдавать отсев после загрузки за фильтрацию при обходе
- упёрся в границу — остановиться и написать в отчёте

ПОСЛЕ ЭТОГО можно возвращаться к дашборду (MEGAPROMPT-hh-final,
Задача 2) — тогда там будет что показывать.
```
