# ПРОМПТ 1 из 5 — Раздел «Мои данные»: контакты и идентичность

Отдельная сессия Claude Code. Делать первым: на этих полях стоят
промпты 2, 3 и 5.

---

```
Прочитай CLAUDE.md, backend/app/db/models.py, backend/app/schemas/profile.py,
backend/app/api/v1/profile.py, frontend/src/.

═══════════════════════════════════════════════════════════════════
ЗАЧЕМ
═══════════════════════════════════════════════════════════════════

Резюме генерируется под вакансию, но контактный блок в нём один и тот же
всегда: ФИО, телефон, почта, GitHub, Telegram, LinkedIn, город. Сейчас
эти данные живут только внутри raw_text распарсенного PDF — то есть их
нельзя ни отредактировать, ни подставить в сгенерированный документ.

ИЗМЕРЕНО: в candidate_profile есть name, headline, seniority, total_years,
summary, locations, relocation, remote_pref, salary_min, salary_currency,
languages, raw_text, embedding, is_active. Полей контактов НЕТ ни одного.

═══════════════════════════════════════════════════════════════════
ЧТО СДЕЛАТЬ
═══════════════════════════════════════════════════════════════════

1. МИГРАЦИЯ. Новая таблица profile_contact (одна к одному с профилем)
   либо колонки в candidate_profile — на твоё усмотрение, но обоснуй
   выбор в докстринге. Поля: полное имя, телефон, email, город,
   и произвольный набор ссылок (github, telegram, linkedin, сайт,
   портфолио) — ссылки списком с типом и адресом, а не фиксированными
   колонками: их набор будет меняться.

2. ЗАПОЛНЕНИЕ ИЗ РЕЗЮМЕ. Экстрактор уже разбирает PDF и кладёт raw_text.
   Достать оттуда контакты при загрузке резюме и предзаполнить — но
   НЕ перезаписывать то, что человек поправил руками. Флаг «правлено
   человеком» на поле.

3. API. GET и PATCH на контакты профиля, рядом с существующим
   PATCH /api/v1/profile/{id}. Валидация: email по форме, телефон
   свободной строкой (форматы разных стран), ссылки — только http(s).

4. ЭКРАН «МОИ ДАННЫЕ» в дашборде. Форма правки, кнопка сохранить.
   Дизайн-система — monopo saigon, описана в MEGAPROMPT-hh-final:
   монохром, поля ввода 0px, кнопки 75px пилюлей, теней нет,
   max-width 1078px.

═══════════════════════════════════════════════════════════════════
ГРАНИЦЫ
═══════════════════════════════════════════════════════════════════

Это персональные данные владельца. Соблюсти существующее правило
проекта про PII в логах: backend/tests/test_pii_logging.py уже
проверяет, что телефоны и почты не утекают в structlog. Новые поля
обязаны быть под этой же защитой — расширить тест, а не обойти его.

Контакты НЕ попадают в эмбеддинги, не участвуют в скоринге и не
уходят ни в один внешний запрос, кроме подстановки в документ,
который генерируется для самого владельца.

DoD: миграция обратима, ruff и mypy --strict чисто, весь прогон
зелёный, тесты без сети, PII-тест расширен на новые поля.
```
