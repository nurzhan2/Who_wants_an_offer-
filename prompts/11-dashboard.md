# ПРОМПТ 5 из 5 — Дашборд целиком

Отдельная сессия Claude Code. Последним: собирает всё, что сделали
промпты 1-4, и закрывает Задачу 2 из MEGAPROMPT-hh-final.

---

```
Прочитай CLAUDE.md, frontend/src/, backend/app/api/v1/,
prompts/hh/hh-final.md (Задача 2 и раздел про дизайн-систему).

═══════════════════════════════════════════════════════════════════
ЧТО ЕСТЬ
═══════════════════════════════════════════════════════════════════

frontend/ — React + Vite + Tailwind + TanStack Query. Одна страница
Dashboard.tsx с единственным HealthBadge. Стек не менять, роутинг добавить.

Данные на 8 сентября 2026: 1174 вакансии, 3938 строк навыков,
582 посчитанных match, один активный профиль владельца.

═══════════════════════════════════════════════════════════════════
ЭКРАНЫ
═══════════════════════════════════════════════════════════════════

ОБЗОР
  вакансий в базе и сколько с эмбеддингами
  позиция обхода по городам и файлам sitemap, сколько осталось
  последний прогон и чем кончился, включая отдельный статус
    «остановлен проверкой на робота» — это не поломка источника
  открытые слаги последнего прогона по каталогу, поимённо: видно,
    выкупались python-страницы или чужие
  откликов отправлено, в очереди, в needs_manual
  Числа из source_state, vacancy, application. Не считать на фронте.

ВАКАНСИИ
  по убыванию score, с разбором: какие требуемые навыки покрыты, какие
  нет. Различать «навык есть, но не назван в этом CV» и «навыка нет».
  Фильтры: город, удалёнка, порог score, источник.
  У каждой строки кнопки из промпта 4: CV и сопроводительное.
  ИЗМЕРЕНО: зарплата отсутствует у большинства вакансий (5 из 6).
  Фильтр по зарплате не должен по умолчанию их выбрасывать.

ОТКЛИКИ
  канбан: queued, needs_manual, sent, и исходы — просмотрен, ожидание,
  приглашение, отказ. По каждому: вакансия, дата, отправленное письмо
  целиком, score, предупреждения hh на момент отправки, исход.
  needs_manual показывает причину: тест, требуется письмо, direct.

ДОКУМЕНТЫ
  сгенерированные CV и письма, к какой вакансии, версия правил,
  ATS-отчёт, отправлено или нет, исход. Здесь видно, работает ли
  контур обратной связи.

МОИ ДАННЫЕ — из промпта 1.
МАСТЕРСКАЯ — из промпта 2.

═══════════════════════════════════════════════════════════════════
ВИД
═══════════════════════════════════════════════════════════════════

Дизайн-система monopo saigon, полностью описана в MEGAPROMPT-hh-final.
Коротко: монохром без единого хроматического цвета в интерфейсе;
Roobert с подстановкой Inter, веса 300/400/600; радиусы только 0px
или 75px, промежуточных нет; теней нет нигде, разделение волосяной
границей 1px и инверсией поверхности; 1078px по центру, section-gap 46px,
card-padding 34px; движение cubic-bezier(0.19, 1, 0.22, 1) 0.8-1.25s.

Три разрешённых конфликта, они там же расписаны: заголовки крупнее 78px
не применять; статусы откликов кодировать положением, подписью и весом
шрифта, а не цветом; score числом и подписью, без цветовой шкалы.

═══════════════════════════════════════════════════════════════════
ГРАНИЦЫ
═══════════════════════════════════════════════════════════════════

Дашборд ТОЛЬКО ЧИТАЕТ и запускает генерацию документов. Отклик
отправляется исключительно через CLI `wwao apply --send`, где есть
подтверждение человеком у клавиатуры. Кнопки «отправить отклик» в
дашборде нет и быть не должно: браузер не даёт той гарантии, ради
которой подтверждение вводилось.

Контакты владельца на экранах показывать, в логи и в аналитику фронта
не отправлять.

DoD: фронт собирается и открывается, все экраны работают на живых
данных, ruff и mypy --strict чисто на бэкенде, весь прогон зелёный,
недостающие эндпоинты дописаны в backend/app/api/v1/.
```
